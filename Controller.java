public class Controller {
    // page 1 ( Login page)
    // page 2 ( Register page)
    // page 3 ( profile info)
    // page 4 ( application comments)
    // page 5 ( welcome page for normal user)
    // page 6 ( welcome page for normal developer)
    // page 7 ( welcome page for normal editor)
    // page 8 ( apps)
    // page 9 ( application information page)
    // page 10 ( upload request page)
    // page 11 ( approve request page)



   public static void main(String[] args){

       // Database connection block





       //

       boolean user_logout = false; // we will add some system logout buttons to some pages.

       while(!user_logout) {

           String page_id = ;// get response from the front-end about page
           boolean page_logout = false;


               if (page_id == 1) {

                   String initial_query = ; // prepare required data
                   // execute query
                   //send data to front-end

                   while(!page_logout) {

                       String button_id = // get button resource

                       if(button_id == ?){
                           String query = "              ";
                           // execute query
                           // send data to front-end
                       }

                       // if button required to log out the page
                       page_logout = true;
                   }
               }

           else if (page_id == 2) {

                   String initial_query = ; // prepare required data
                   // execute query
                   //send data to front-end

                   while(!page_logout) {

                       String button_id = // get button resource

                       if(button_id == ?){
                           String query = "              ";
                           // execute query
                           // send data to front-end
                       }

                       // if button required to log out the page
                       page_logout = true;
                   }
           }

           else if (page_id == 3) {

                   String initial_query = ; // prepare required data
                   // execute query
                   //send data to front-end

                   while(!page_logout) {

                       String button_id = // get button resource

                       if(button_id == ?){
                           String query = "              ";
                           // execute query
                           // send data to front-end
                       }

                       // if button required to log out the page
                       page_logout = true;
                   }
           }

           else if (page_id == 4) {

                   String initial_query = ; // prepare required data
                   // execute query
                   //send data to front-end

                   while(!page_logout) {

                       String button_id = // get button resource

                       if(button_id == ?){
                           String query = "              ";
                           // execute query
                           // send data to front-end
                       }

                       // if button required to log out the page
                       page_logout = true;
                   }
           }

           else if (page_id == 5) {

                   String initial_query = ; // prepare required data
                   // execute query
                   //send data to front-end

                   while(!page_logout) {

                       String button_id = // get button resource

                       if(button_id == ?){
                           String query = "              ";
                           // execute query
                           // send data to front-end
                       }

                       // if button required to log out the page
                       page_logout = true;
                   }
           }

           else if (page_id == 6) {

                   String initial_query = ; // prepare required data
                   // execute query
                   //send data to front-end

                   while(!page_logout) {

                       String button_id = // get button resource

                       if(button_id == ?){
                           String query = "              ";
                           // execute query
                           // send data to front-end
                       }

                       // if button required to log out the page
                       page_logout = true;
                   }
           }

           else if (page_id == 7) {

                   String initial_query = ; // prepare required data
                   // execute query
                   //send data to front-end

                   while(!page_logout) {

                       String button_id = // get button resource

                       if(button_id == ?){
                           String query = "              ";
                           // execute query
                           // send data to front-end
                       }

                       // if button required to log out the page
                       page_logout = true;
                   }
           }

           else if (page_id == 8) {

                   String initial_query = ; // prepare required data
                   // execute query
                   //send data to front-end

                   while(!page_logout) {

                       String button_id = // get button resource

                       if(button_id == ?){
                           String query = "              ";
                           // execute query
                           // send data to front-end
                       }

                       // if button required to log out the page
                       page_logout = true;
                   }
           }

           else if (page_id == 9) {

                   String initial_query = ; // prepare required data
                   // execute query
                   //send data to front-end

                   while(!page_logout) {

                       String button_id = // get button resource

                       if(button_id == ?){
                           String query = "              ";
                           // execute query
                           // send data to front-end
                       }

                       // if button required to log out the page
                       page_logout = true;
                   }
           }

           else if (page_id == 10) {

                   String initial_query = ; // prepare required data
                   // execute query
                   //send data to front-end

                   while(!page_logout) {

                       String button_id = // get button resource

                       if(button_id == ?){
                           String query = "              ";
                           // execute query
                           // send data to front-end
                       }

                       // if button required to log out the page
                       page_logout = true;
                   }
           }

           else if (page_id == 11) {

                   String initial_query = ; // prepare required data
                   // execute query
                   //send data to front-end

                   while(!page_logout) {

                       String button_id = // get button resource

                       if(button_id == ?){
                           String query = "              ";
                           // execute query
                           // send data to front-end
                       }

                       // if button required to log out the page
                       page_logout = true;
                   }
           }
            //.................

       }

   }
}
